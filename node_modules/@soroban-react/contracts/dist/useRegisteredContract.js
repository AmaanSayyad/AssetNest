"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.useRegisteredContract = exports.useWrappedContract = void 0;
const core_1 = require("@soroban-react/core");
const react_1 = require("react");
const contractInvoke_1 = require("./contractInvoke");
/**
 * Returns the deployment information for the given contract ID and network passphrase.
 * @param deployments - The array of contract deployment information.
 * @param contractId - The ID of the contract.
 * @param networkPassphrase - The network passphrase.
 * @returns The deployment information for the contract.
 * @throws If the deployment is not found.
 */
const getDeployment = (deployments, contractId, networkPassphrase) => {
    let deployment = deployments.find(deployment => {
        return (deployment.contractId.toLowerCase() === contractId.toLowerCase() &&
            deployment.networkPassphrase.toLowerCase() ===
                (networkPassphrase || '').toLowerCase());
    });
    return deployment;
};
/**
 * React hook that returns a `WrappedContract` object configured with
 * the provided deployment information.
 * @param deploymentInfo - The deployment information for the contract.
 * @returns The `WrappedContract` object.
 */
const useWrappedContract = (deploymentInfo) => {
    const sorobanContext = (0, core_1.useSorobanReact)();
    const [wrappedContract, setwWrappedContract] = (0, react_1.useState)(undefined);
    const wrappedInvokeFunction = (0, react_1.useCallback)((args) => __awaiter(void 0, void 0, void 0, function* () {
        const contractInvokeArgs = Object.assign(Object.assign({}, args), { sorobanContext, contractAddress: deploymentInfo === null || deploymentInfo === void 0 ? void 0 : deploymentInfo.contractAddress });
        return (0, contractInvoke_1.contractInvoke)(contractInvokeArgs);
    }), [sorobanContext, deploymentInfo]);
    (0, react_1.useEffect)(() => {
        if (!deploymentInfo) {
            setwWrappedContract(undefined);
            return;
        }
        const newWrappedContract = {
            deploymentInfo,
            invoke: wrappedInvokeFunction,
        };
        setwWrappedContract(newWrappedContract);
    }, [deploymentInfo, wrappedInvokeFunction]);
    return wrappedContract;
};
exports.useWrappedContract = useWrappedContract;
/**
 * React hook that returns a `WrappedContract` object for the given contract ID,
 * looked up from the deployments registry for the active chain.
 * @param contractId - The ID of the contract.
 * @returns The `WrappedContract` object.
 */
const useRegisteredContract = (contractId) => {
    const { deployments, activeChain } = (0, core_1.useSorobanReact)();
    let networkPassphrase = (activeChain === null || activeChain === void 0 ? void 0 : activeChain.networkPassphrase) || '';
    const deploymentInfo = getDeployment(deployments || [], contractId, networkPassphrase);
    return (0, exports.useWrappedContract)(deploymentInfo);
};
exports.useRegisteredContract = useRegisteredContract;
