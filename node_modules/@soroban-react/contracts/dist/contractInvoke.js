"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.contractInvoke = contractInvoke;
const StellarSdk = __importStar(require("@stellar/stellar-sdk"));
const stellar_sdk_1 = require("@stellar/stellar-sdk");
const contractTransaction_1 = require("./contractTransaction");
const transaction_1 = require("./transaction");
let xdr = StellarSdk.xdr;
// Dummy source account for simulation. The public key for this is all 0-bytes.
const defaultAddress = 'GAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAWHF';
/**
 * Invokes a smart contract method.
 * @param {InvokeArgs} args - Arguments for invoking the smart contract.
 * @returns {Promise<TxResponse | StellarSdk.xdr.ScVal>} - A promise resolving to the transaction response or the result of the simulation.
 * @throws {Error} - If there are errors during the contract invocation process.
 */
function contractInvoke(_a) {
    return __awaiter(this, arguments, void 0, function* ({ contractAddress, method, args = [], signAndSend = false, fee = 100, skipAddingFootprint, secretKey, sorobanContext, reconnectAfterTx = true, timeoutSeconds = 20, }) {
        const { server, address, activeChain } = sorobanContext;
        if (!activeChain) {
            throw new Error('No active Chain');
        }
        if (!server) {
            throw new Error('No connected to a Server');
        }
        if (signAndSend && !secretKey && !sorobanContext.activeConnector) {
            throw new Error('contractInvoke: You are trying to sign a txn without providing a source, secretKey or active connector');
        }
        const networkPassphrase = activeChain === null || activeChain === void 0 ? void 0 : activeChain.networkPassphrase;
        let source = null;
        if (secretKey) {
            source = yield server.getAccount(StellarSdk.Keypair.fromSecret(secretKey).publicKey());
        }
        else {
            try {
                if (!address)
                    throw new Error('No address');
                source = yield server.getAccount(address);
            }
            catch (error) {
                source = new StellarSdk.Account(defaultAddress, '0');
            }
        }
        //Builds the transaction
        let txn = (0, contractTransaction_1.contractTransaction)({
            source,
            networkPassphrase,
            contractAddress,
            method,
            args,
        });
        const simulated = yield (server === null || server === void 0 ? void 0 : server.simulateTransaction(txn));
        if (stellar_sdk_1.SorobanRpc.Api.isSimulationError(simulated)) {
            throw new Error(simulated.error);
        }
        else if (!simulated.result) {
            throw new Error(`invalid simulation: no result in ${simulated}`);
        }
        if (!signAndSend && simulated) {
            return simulated.result.retval;
        }
        else {
            // If signAndSend
            const res = yield (0, transaction_1.signAndSendTransaction)({
                txn,
                skipAddingFootprint,
                secretKey,
                sorobanContext,
                timeoutSeconds,
            });
            if (reconnectAfterTx) {
                sorobanContext.connect();
            }
            return res;
        }
    });
}
